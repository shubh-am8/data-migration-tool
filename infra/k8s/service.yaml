apiVersion: v1
kind: Service
metadata:
  name: migration-platform
  labels:
    app: migration-platform
spec:
  type: ClusterIP
  selector:
    app: migration-platform
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
