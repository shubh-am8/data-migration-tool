# Frontend Dashboard

Next.js dashboard for the Data Migration Platform.

See [Frontend Dashboard](../../docs/components/frontend.md) for routes, shared UI patterns, and API client usage.

Run the full stack from the repo root:

```bash
./run-local-dev.sh
```

Open http://localhost:3000
